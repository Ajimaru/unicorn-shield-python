# Pinned to the newest releases that still run on the target hardware:
# a Raspberry Pi 1 Model B+ (armv6l) on Raspbian 10 buster, whose system
# interpreter is Python 3.7.3.
#
# These are compatibility constraints, not staleness. Newer majors are not
# installable there at all:
#   Flask 3.x        requires_python >=3.9   (2.2.5 is the last for 3.7)
#   Flask-Cors 5.0.1+ requires_python >=3.9   (5.0.0 is the last for 3.7)
# See .github/dependabot.yml for why pip is not watched by Dependabot.
#
# Security note: this set closes CVE-2023-30861 (Flask) and CVE-2024-1681,
# CVE-2024-6221, CVE-2024-6839, CVE-2024-6844, CVE-2024-6866 (Flask-Cors).
# CVE-2026-27205 (LOW, Flask < 3.1.3) cannot be fixed on Python 3.7; it needs
# session cookies to be exploitable and this API uses none.
Flask==2.2.5
Flask-Cors==5.0.0
Werkzeug==2.2.3
